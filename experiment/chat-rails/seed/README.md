# Browser chat fixture

This small app has a browser client, a Node HTTP server, and a fake
token generator. It uses only the Node standard library.

Run `npm test` for the requirements introduced so far. Run `npm start`
and open `http://localhost:3000` to use the browser client.
